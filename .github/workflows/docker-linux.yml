name: Linux

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  release:
    types: [ published, created ] # Triggers the build when a release is created or published
  workflow_dispatch: 

jobs:
  build-cts:
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      
    # Define the execution matrix for 64-bit and 32-bit targets
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: "x86_64"
            output_dir: "build-output-64bit"
            tarball_name: "vk-gl-cts-linux-x86_64.tar.gz"
            setup_cmd: "echo 'Building for 64-bit...'"
            # FIX: Explicitly disable zlib-ng / third-party hardware optimizations and strictly block advanced SIMD emission via compiler flags
            cmake_flags: "-DCMAKE_C_FLAGS='-march=x86-64 -mno-ssse3 -mno-sse4.1 -mno-sse4.2 -mno-avx -mno-avx2 -mno-avx512f' -DCMAKE_CXX_FLAGS='-march=x86-64 -mno-ssse3 -mno-sse4.1 -mno-sse4.2 -mno-avx -mno-avx2 -mno-avx512f' -DENABLE_AVX=OFF -DENABLE_AVX2=OFF -DENABLE_AVX512=OFF -DWITH_AVX=OFF -DWITH_AVX2=OFF -DWITH_AVX512=OFF -DWITH_AVX512BW=OFF -DWITH_SSSE3=OFF -DWITH_SSE4=OFF"
            packages: "libpng-dev libz-dev libx11-dev libxcb1-dev libxrender-dev libvulkan-dev"

          - arch: "i686"
            output_dir: "build-output-32bit"
            tarball_name: "vk-gl-cts-linux-i686.tar.gz"
            setup_cmd: "dpkg --add-architecture i386 && apt-get update"
            # FIX: Completely strip all SIMD generation down to base i686 and turn off third-party vector test variables
            cmake_flags: "-DCMAKE_C_FLAGS='-m32 -march=i686 -mno-sse -mno-sse2 -mno-ssse3 -mno-sse4.1 -mno-sse4.2 -mno-avx -mno-avx2 -mno-avx512f' -DCMAKE_CXX_FLAGS='-m32 -march=i686 -mno-sse -mno-sse2 -mno-ssse3 -mno-sse4.1 -mno-sse4.2 -mno-avx -mno-avx2 -mno-avx512f' -DENABLE_AVX=OFF -DENABLE_AVX2=OFF -DENABLE_AVX512=OFF -DWITH_AVX=OFF -DWITH_AVX2=OFF -DWITH_AVX512=OFF -DWITH_AVX512BW=OFF -DWITH_SSSE3=OFF -DWITH_SSE4=OFF"
            packages: "gcc-multilib g++-multilib libpng-dev:i386 libz-dev:i386 libx11-dev:i386 libxcb1-dev:i386 libxrender-dev:i386 libvulkan-dev:i386"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build CTS inside Ubuntu Container via Docker Run
        run: |
          # Create architecture-specific output directory on the host
          mkdir -p ${{ github.workspace }}/${{ matrix.output_dir }}

          docker run --rm \
            -v "${{ github.workspace }}:/work" \
            -w /work \
            ubuntu:22.04 \
            /bin/bash -c "
              set -e
              
              # 1. Execute specific architecture setup (e.g., enabling i386)
              ${{ matrix.setup_cmd }}
              
              # 2. Update packages and install target-specific dependencies
              apt-get update && apt-get install -y --no-install-recommends \
                build-essential cmake ninja-build git python3 python3-pip ca-certificates \
                ${{ matrix.packages }}

              # 3. Fetch external sources using the Khronos script
              python3 external/fetch_sources.py
              
              # 4. Configure and compile the project using appropriate flags
              mkdir build && cd build
              cmake .. -G Ninja \
                -DCMAKE_BUILD_TYPE=Release \
                -DDEQP_TARGET=null \
                -DSELECTED_BUILD_TARGETS='deqp-vk;deqp-gles2;deqp-gles3;glcts' \
                ${{ matrix.cmake_flags }}
              ninja
              
              # 5. Copy the compiled binaries to the mapped host volume
              cp -r external/vulkancts/modules/vulkan/deqp-vk /work/${{ matrix.output_dir }}/
              cp -r external/openglcts/modules/glcts /work/${{ matrix.output_dir }}/
            "

      - name: Fix Permissions, Pack and Compress
        run: |
          # Fix file permissions (processes inside the container run as root)
          sudo chown -R $USER:$USER ${{ github.workspace }}/${{ matrix.output_dir }}
          
          cd ${{ matrix.output_dir }}
          tar -czf ../${{ matrix.tarball_name }} .
          cd ..

      - name: Upload Packaged Binaries to GitHub Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.tarball_name }}
          path: ${{ matrix.tarball_name }}
          archive: false # FIX: Prevents GitHub from wrapping the .tar.gz file inside a secondary ZIP file

      - name: Upload Binaries directly to GitHub Release
        # This step runs ONLY if the workflow was triggered by a Release event
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.tarball_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
